## setState
* change state based on new value: objecty
* change state based on previouse state: arrow function
* immutable state
    * shallow merge

## Data flow
* top-down, unidirectional
    * state is encapsulated in its own and can only pass down to the children components
